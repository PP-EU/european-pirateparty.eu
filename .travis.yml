language: python
python: 3.6
install: "pip install Lektor"
script: "lektor build -O web ; git init; git checkout -b gh-pages ;git add web; git commit -m whatever;git push -f -q https://LEKTOR_DEPLOY_USERNAME:LEKTOR_DEPLOY_PASSWORDY@github.com/LEKTOR_DEPLOY_USERNAME/european-pirateparty.eu-gh-pages gh-pages &>/dev/null"
deploy:
  provider: script
  script: "lektor deploy ghpages"
